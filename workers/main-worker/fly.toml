# workers/main-worker/fly.toml - Fleet worker Fly.io config per PDF Day-One #10.
app = "fleet-pilot-worker"
primary_region = "sjc"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
