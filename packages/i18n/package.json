{
  "name": "@fleet/i18n",
  "version": "0.1.0",
  "private": true,
  "description": "Message SSOT (Zod-validated locale vocabulary): the Vietnamese and English dictionaries every surface reads. Prescribed by the Frozen Stack as @i18n and consumed by ops-web plus the React Native apps, so a dispatcher-facing label exists in exactly one place. Vietnamese values are immutable production contracts.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist *.tsbuildinfo && tsc -p tsconfig.build.json",
    "check": "pnpm lint && pnpm typecheck && pnpm test",
    "clean": "rm -rf dist .turbo *.tsbuildinfo coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:unit": "vitest run",
    "test:watch": "vitest --watch",
    "typecheck": "tsc -p tsconfig.json"
  },
  "engines": {
    "node": ">=22.0.0 <25.0.0"
  },
  "dependencies": {
    "zod": "catalog:"
  }
}
