{
  "name": "@fleet/observability",
  "version": "0.1.0",
  "private": true,
  "description": "Shared observability primitives: PII scrubber for Sentry events across api, ops-web, driver-app",
  "sideEffects": false,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "node -e \"const fs=require('fs');fs.rmSync('dist',{recursive:true,force:true});for(const f of fs.readdirSync('.'))if(f.endsWith('.tsbuildinfo'))fs.rmSync(f,{force:true});\" && tsc -p tsconfig.build.json",
    "check": "echo check is a turbo aggregate task -- run: pnpm exec turbo run check --filter=@fleet/observability",
    "clean": "rm -rf dist .turbo *.tsbuildinfo coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:unit": "vitest run",
    "test:watch": "vitest --watch",
    "typecheck": "tsc -p tsconfig.json",
    "test:mutation": "stryker run"
  },
  "engines": {
    "node": ">=22.0.0 <25.0.0"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/typescript-checker": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "fast-check": "^4.7.0",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "zod": "catalog:"
  }
}
