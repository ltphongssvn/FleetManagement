{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile && pnpm --filter @fleet/sync-protocol --filter @fleet/observability --filter @fleet/domain build && cd apps/driver-app && pnpm expo export --platform web"
  },
  "deploy": {
    "startCommand": "npx serve dist -p ${PORT:-8081}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
