{
  "description": "Forbidden-topology hashes for local-secret-guard. Each entry is the SHA-256 of a lowercased PRODUCTION hostname that must never appear in tracked source. Hashes, not plaintext, so this config cannot itself leak the topology it protects. Regenerate with: pnpm run guard:local-secrets:baseline",
  "forbiddenHostSha256": [
    {
      "sha256": "43ef0d5e8ef357cc7ed3b2cc38a5cfe2ddc5d9f96810277dc4146e4a6fee8782",
      "note": "production database public proxy endpoint"
    }
  ]
}
